"""warm_minimal_symbolic_ban_ghost_state

暖白极简科普风格修复：实测发现"幽灵态"（半透明保留失效元素）被当作退场的
默认做法，导致失效/被否定的元素长期占据画面空间，叠加缩小对照后同屏元素
持续堆积、互相遮挡（截图案例：会前/会后多层卡片与人物图标重叠）。

- 色彩系统：删除"幽灵态优先于删除"的表述，语义强调色只保留砖红/青绿两个
  固定色
- 动画系统：状态变化规则删除幽灵态，废弃元素统一走退场（FadeOut）；
  退场规则从"仅限切换镜头时"扩展到"镜头内部一旦某元素完成其作用即退场"；
  对比/取舍场景里被否定一方改为划线/打叉，是否退场取决于该元素后续是否
  还被需要，而非机械立即退场；新增"缩小对照"使用红线——只用于同屏两个
  当前活跃元素的即时比例对比，对比结束后必须 Transform 为正式主体或退场，
  禁止缩小后停留在角落充当存档标记

原地更新两个组件（UUID 不变），downgrade 完整还原修改前文本。

Revision ID: e3a9f61c8b2d
Revises: 82f4c6a9d731
Create Date: 2026-08-03 00:00:00.000000

"""
from datetime import datetime, timezone
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# revision identifiers, used by Alembic.
revision: str = "e3a9f61c8b2d"
down_revision: Union[str, Sequence[str], None] = "82f4c6a9d731"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


COMPONENT_IDS = {
    "color_scheme": "b7a2f8c1-1002-4000-8000-000000000001",
    "animation": "b7a2f8c1-1002-4000-8000-000000000005",
}

NEW = {
    "color_scheme": {
        "name": "暖白极简·多彩符号",
        "description": "暖象牙白底、多彩循环主体色板、语义固定强调色与饱满构图规则（禁用幽灵态）",
        "prompt_text": """【视觉系统：暖白极简·多彩符号】

一、背景基调
全片统一使用暖象牙白 #FAF7F0 背景，不使用纯白或深色背景，营造温纸质感。
画面保持约 30%-40% 负空间，每个镜头只有一个主要视觉中心。
留白必须是"有秩序的留白"——由标题、主体、标注三层撑起画面骨架后剩余的空间，
而不是元素过小、过少造成的空旷。

二、主体色板（用于图形元素、图标、数据系列）
- 珊瑚橙 #E8714A
- 青绿 #3DAA8C
- 蓝紫 #8B7BB5
- 宝蓝 #5B8DD9
- 金黄 #F0B429
同类元素成组出现时在主体色板内循环取色，形成多彩但和谐的群体感。
需要区分"主角与群众"时，主角用宝蓝或金黄并放大，群众用其余色循环。

三、语义强调色（含义固定，不得挪用）
- 砖红 #C4614A：代价、错误、约束、失败、警示
- 青绿 #3DAA8C：正确、成功、结论、正向方向
- 禁用幽灵态：任何元素一旦被否定、失效或不再被论证需要，一律用退场
  （FadeOut）处理，禁止用半透明/降低 fill_opacity 的方式让它继续留在
  画面上充当"已排除"的记号——半透明元素仍占据画面空间，是画面元素
  重叠拥挤的主要成因

四、文字色
- 标题：依语义取砖红（困境/警示类）、青绿（结论/方法类）或深灰 #3C3C3C（中性）
- 辅助标注：暖灰 #8A7A6A，小字号，永远不与标题争夺注意力
- 结构线（分隔线、基准线、虚线框）：暖灰 #C0B0A0

五、页面元素与构图
每个镜头必须具备三层画面骨架，缺一即显空旷：
- 标题层：顶部居中或左上放置本镜头主题短语（不超过 10 字），
  除刻意留白的收尾镜头外每个镜头都有标题；关键标题下方可配细分隔线
- 主体层：核心图形整体占画面宽度的 50%-70%、高度的 30%-50%，
  居于画面中部；禁止主体缩成一条细带或一个小角落；页面元素要丰富，不可过于空旷
- 标注层：主体的关键部位配 2-4 个短标注（关键词、数字、小箭头），
  标注紧贴其锚定元素，不悬空

元素尺度规则：
- 单个图形元素的最小可辨识尺寸不低于画面高度的 4%；
  点阵/队列类元素单点直径不低于画面高度的 2%
- 群体元素数量以一眼可感知为度（一排不超过 20 个，矩阵不超过 8×8），
  数量本身是信息时优先用"少量大元素 + 计数数字"代替海量小元素
- 相邻元素间距 0.5-1.5 个元素宽度：既不粘连也不稀疏

区域与结构支撑：
- 画面出现分区语义（阶段、阵营、观察/选择区）时，必须用可见结构表达：
  虚线区域框、浅色底色块（填充透明度 0.08-0.15）或分隔线 + 区域标签，
  不能只靠元素颜色差异暗示分区
- 主体元素后方可放置同色系大圆或圆角矩形光晕衬底（填充透明度约 0.12）
  增加画面层次感和厚度

六、总原则
一屏之内主体色不超过 4 种加 1 个语义强调色。
色彩本身承载信息（状态、阵营、阶段），禁止纯装饰性用色。
同一概念跨镜头保持同色。
构图自检：若移除标题和标注后画面仍能成立，说明标注层缺失；
若主体缩到画面 1/4 以下，说明尺度失衡，必须放大主体或增加结构支撑。""",
    },
    "animation": {
        "name": "暖白极简·原地生长动效",
        "description": "原地 grow-in 入场、禁用幽灵态改用即时退场、三层出场秩序的克制动画语言",
        "prompt_text": """【动画系统：暖白极简·原地生长动效】

总原则：动画表达一次状态或信息的变化。

一、视觉层次
每个镜头分三层构建：结构层（分隔线/区域框/坐标）→ 主体层（图形/图标/数据）
→ 标注层（关键词/数字/箭头）。出场顺序：结构层 → 主体层 → 标注层。
标题固定于顶部，主体集中于中部区域。

二、入场规则
- 图形元素：在最终位置原地从零缩放放大出现（grow-in），禁止从屏幕外滑入
- 群体元素（同类元素排/阵）：按空间顺序依次 grow-in，错落间隔 0.1-0.2 秒，
  形成波浪式入场；单个元素约 0.3 秒
- 需要强调的单个主体：独立入场，约 0.5 秒，可配轻微回弹
- 标题与文字：淡入，约 0.4 秒；标注在其锚定元素出现后延迟约 0.2 秒跟随出现
- 关键数字：单独入场并适度放大定格，与旁白说出该数字的时刻对齐

三、状态变化规则（表达信息演化的核心手段）
- 禁用幽灵态：不允许用降低 fill_opacity 的方式表示"未知/已排除/过去/失效"，
  该表现手法一律替换为退场（见第六条）
- 选中/强调：放大 1.2-1.4 倍加颜色加深，可加同色外圈描边
- 转移焦点：旧焦点缩回原尺寸恢复常态色，新焦点同时放大，形成"接力"
- 群体状态批量变化：按空间顺序错落进行，呈扫过效果
- 对比/取舍：被否定一方用划线或打叉表达否定（不使用幽灵态），被肯定一方
  高亮，二者动画同时进行；被否定元素是否退场取决于它后续是否还被需要——
  仍作为参照物或将被再次提及则保留在画面，确认不再需要则立即退场，
  不得因"已经处理过对比"就默认让它无限期留在画面上
- 缩小对照：仅用于同屏内两个当前活跃元素的即时比例/量级对比（如二选一
  选项对比、正确方案与错误方案对比），使用后二选一——比较结束立即
  Transform 为下一步的正式主体，或立即退场；禁止缩小后长期停留在
  画面角落充当"已完成"的存档标记
- 退场：不止切换镜头时——镜头内部只要某个元素已完成其论证/对比/铺垫
  作用且明确不再需要，必须当场 FadeOut 退场，不得拖到镜头结束或
  切换镜头才处理，避免同屏堆积互相遮挡

四、结构元素动画
- 分隔线/基准线：沿自身方向生长绘制，0.5-0.6 秒，画完后再出现两侧标签
- 区域框：先描边后填充，填充透明度不超过 0.15，约 0.7 秒
- 箭头：从起点向终点生长，约 0.4 秒，仅用于表达因果或指向
- 图表（饼图/柱状/进度）：数值从零生长到目标值，约 1 秒，
  让观众看到量的形成过程

五、beat 内时间分配
- 每个 beat 的主要视觉变化在该 beat 时间窗内完成，动作间可留 0.3-0.5 秒静置
- 首个 beat 完成结构层与主体层首批元素；后续 beat 只做增量
  （新增元素或状态变化）
- 相邻 beat 优先通过已有元素的变色、变形、移动、放缩衔接，避免清场重绘
- 镜头末尾保持最终画面静置，禁止补时用的空等待或循环动画

六、跨镜头连续性
- 同一幕内相邻镜头复用上一镜头的元素变量，元素空间位置保持一致
- 贯穿多镜头的固定角色/参照物，位置全片固定不移动
- 清场仅发生在幕间切换，用整体淡出完成，不逐个删除""",
    },
}

OLD = {
    "color_scheme": {
        "name": "暖白极简·多彩符号",
        "description": "暖象牙白底、多彩循环主体色板、语义固定强调色、幽灵态状态语言与饱满构图规则",
        "prompt_text": """【视觉系统：暖白极简·多彩符号】

一、背景基调
全片统一使用暖象牙白 #FAF7F0 背景，不使用纯白或深色背景，营造温纸质感。
画面保持约 30%-40% 负空间，每个镜头只有一个主要视觉中心。
留白必须是"有秩序的留白"——由标题、主体、标注三层撑起画面骨架后剩余的空间，
而不是元素过小、过少造成的空旷。

二、主体色板（用于图形元素、图标、数据系列）
- 珊瑚橙 #E8714A
- 青绿 #3DAA8C
- 蓝紫 #8B7BB5
- 宝蓝 #5B8DD9
- 金黄 #F0B429
同类元素成组出现时在主体色板内循环取色，形成多彩但和谐的群体感。
需要区分"主角与群众"时，主角用宝蓝或金黄并放大，群众用其余色循环。

三、语义强调色（含义固定，不得挪用）
- 砖红 #C4614A：代价、错误、约束、失败、警示
- 青绿 #3DAA8C：正确、成功、结论、正向方向
- 幽灵态：任意元素降为 fill_opacity 0.15-0.2 表示"未知/已排除/过去/失效"，
  这是表达状态退场的首选方式，优先于直接删除元素。

四、文字色
- 标题：依语义取砖红（困境/警示类）、青绿（结论/方法类）或深灰 #3C3C3C（中性）
- 辅助标注：暖灰 #8A7A6A，小字号，永远不与标题争夺注意力
- 结构线（分隔线、基准线、虚线框）：暖灰 #C0B0A0

五、页面元素与构图
每个镜头必须具备三层画面骨架，缺一即显空旷：
- 标题层：顶部居中或左上放置本镜头主题短语（不超过 10 字），
  除刻意留白的收尾镜头外每个镜头都有标题；关键标题下方可配细分隔线
- 主体层：核心图形整体占画面宽度的 50%-70%、高度的 30%-50%，
  居于画面中部；禁止主体缩成一条细带或一个小角落；页面元素要丰富，不可过于空旷
- 标注层：主体的关键部位配 2-4 个短标注（关键词、数字、小箭头），
  标注紧贴其锚定元素，不悬空

元素尺度规则：
- 单个图形元素的最小可辨识尺寸不低于画面高度的 4%；
  点阵/队列类元素单点直径不低于画面高度的 2%
- 群体元素数量以一眼可感知为度（一排不超过 20 个，矩阵不超过 8×8），
  数量本身是信息时优先用"少量大元素 + 计数数字"代替海量小元素
- 相邻元素间距 0.5-1.5 个元素宽度：既不粘连也不稀疏

区域与结构支撑：
- 画面出现分区语义（阶段、阵营、观察/选择区）时，必须用可见结构表达：
  虚线区域框、浅色底色块（填充透明度 0.08-0.15）或分隔线 + 区域标签，
  不能只靠元素颜色差异暗示分区
- 主体元素后方可放置同色系大圆或圆角矩形光晕衬底（填充透明度约 0.12）
  增加画面层次感和厚度

六、总原则
一屏之内主体色不超过 4 种加 1 个语义强调色。
色彩本身承载信息（状态、阵营、阶段），禁止纯装饰性用色。
同一概念跨镜头保持同色。
构图自检：若移除标题和标注后画面仍能成立，说明标注层缺失；
若主体缩到画面 1/4 以下，说明尺度失衡，必须放大主体或增加结构支撑。""",
    },
    "animation": {
        "name": "暖白极简·原地生长动效",
        "description": "原地 grow-in 入场、幽灵态代替删除、三层出场秩序的克制动画语言",
        "prompt_text": """【动画系统：暖白极简·原地生长动效】

总原则：动画表达一次状态或信息的变化。

一、视觉层次
每个镜头分三层构建：结构层（分隔线/区域框/坐标）→ 主体层（图形/图标/数据）
→ 标注层（关键词/数字/箭头）。出场顺序：结构层 → 主体层 → 标注层。
标题固定于顶部，主体集中于中部区域。

二、入场规则
- 图形元素：在最终位置原地从零缩放放大出现（grow-in），禁止从屏幕外滑入
- 群体元素（同类元素排/阵）：按空间顺序依次 grow-in，错落间隔 0.1-0.2 秒，
  形成波浪式入场；单个元素约 0.3 秒
- 需要强调的单个主体：独立入场，约 0.5 秒，可配轻微回弹
- 标题与文字：淡入，约 0.4 秒；标注在其锚定元素出现后延迟约 0.2 秒跟随出现
- 关键数字：单独入场并适度放大定格，与旁白说出该数字的时刻对齐

三、状态变化规则（表达信息演化的核心手段）
- 排除/失效/过去：原地降为幽灵态（fill_opacity 0.15-0.2），保留轮廓不删除
- 选中/强调：放大 1.2-1.4 倍加颜色加深，可加同色外圈描边
- 转移焦点：旧焦点缩回原尺寸恢复常态色，新焦点同时放大，形成"接力"
- 群体状态批量变化：按空间顺序错落进行，呈扫过效果
- 对比/取舍：被否定一方变幽灵态或划线，被肯定一方高亮，二者动画同时进行
- 退场：当切换镜头时，不再使用的元素务必退场，避免与下一镜头产生遮挡

四、结构元素动画
- 分隔线/基准线：沿自身方向生长绘制，0.5-0.6 秒，画完后再出现两侧标签
- 区域框：先描边后填充，填充透明度不超过 0.15，约 0.7 秒
- 箭头：从起点向终点生长，约 0.4 秒，仅用于表达因果或指向
- 图表（饼图/柱状/进度）：数值从零生长到目标值，约 1 秒，
  让观众看到量的形成过程

五、beat 内时间分配
- 每个 beat 的主要视觉变化在该 beat 时间窗内完成，动作间可留 0.3-0.5 秒静置
- 首个 beat 完成结构层与主体层首批元素；后续 beat 只做增量
  （新增元素或状态变化）
- 相邻 beat 优先通过已有元素的变色、变形、移动、放缩衔接，避免清场重绘
- 镜头末尾保持最终画面静置，禁止补时用的空等待或循环动画

六、跨镜头连续性
- 同一幕内相邻镜头复用上一镜头的元素变量，元素空间位置保持一致
- 贯穿多镜头的固定角色/参照物，位置全片固定不移动
- 清场仅发生在幕间切换，用整体淡出完成，不逐个删除""",
    },
}


def _upsert(texts: dict) -> None:
    conn = op.get_bind()
    now = datetime.now(timezone.utc)
    prompt_components_table = sa.table(
        "prompt_components",
        sa.column("id", postgresql.UUID(as_uuid=True)),
        sa.column("category", sa.String),
        sa.column("name", sa.String),
        sa.column("description", sa.Text),
        sa.column("prompt_text", sa.Text),
        sa.column("is_builtin", sa.Boolean),
        sa.column("created_by", sa.String),
        sa.column("created_at", sa.DateTime(timezone=True)),
        sa.column("updated_at", sa.DateTime(timezone=True)),
    )
    category_map = {"color_scheme": "color_scheme", "animation": "animation_style"}
    for key, fields in texts.items():
        stmt = postgresql.insert(prompt_components_table).values(
            id=COMPONENT_IDS[key],
            category=category_map[key],
            name=fields["name"],
            description=fields["description"],
            prompt_text=fields["prompt_text"],
            is_builtin=True,
            created_by=None,
            created_at=now,
            updated_at=now,
        )
        stmt = stmt.on_conflict_do_update(
            index_elements=["id"],
            set_={
                "category": stmt.excluded.category,
                "name": stmt.excluded.name,
                "description": stmt.excluded.description,
                "prompt_text": stmt.excluded.prompt_text,
                "is_builtin": stmt.excluded.is_builtin,
                "updated_at": stmt.excluded.updated_at,
            },
        )
        conn.execute(stmt)


def upgrade() -> None:
    _upsert(NEW)


def downgrade() -> None:
    _upsert(OLD)
